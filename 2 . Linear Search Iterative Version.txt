#include <iostream>using namespace std;bool linear_search(float array[],int last, float target,int &location){	int looker=0	bool is_found=false;	while(looker<last && list[looker]!=target)	{		looker+=1;	}	if(array[looker]==target){			location=looker;			is_found=true;	return is_found;}int main(){	int number_of_elements;	cout<<"ENTER THE NUMBER OF ELEMENTS FOR THE ARRAY :";	cin>>number_of_elements;	float array[number_of_elements];	for(int element_index=0;element_index<number_of_elements;element_index++){		cout<<"ENTER VALUES:";				cin>>array[element_index];		}	float target;	cout<<"ENTER THE ELEMENT TO BE FOUND :";	cin>>target;	int location;	bool found = linear_search(array,number_of_elements-1,target,location);	if(found==true)		cout<<"THE ELEMENT WAS FOUND IN :"<<location;	else		cout<<"THE ELEMENT WASN'T FOUND IN THE ARRAY. "<<location;	return 0;}